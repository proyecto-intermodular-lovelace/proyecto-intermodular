-- scripts/db/002_suppliers.sql
-- OBSOLETO: seed con esquema incompatible (columnas en español).
-- La tabla suppliers se define en 001_init.sql (columnas en inglés).
-- El seed correcto está en 006_seed_suppliers.sql.
-- Mantenido como no-op para no alterar el orden de ejecución.

BEGIN;
-- (no-op)
COMMIT;